{
  "name": "@oceanfresh/cart",
  "version": "0.0.0",
  "private": true,
  "description": "OceanFresh Cart Domain \u2014 Clean Architecture repository, service, hooks, queries, mutations, and components",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./repository": "./src/repository/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@oceanfresh/shared": "workspace:*",
    "@oceanfresh/supabase": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "vitest": "^2.0.3"
  }
}
