{
  "name": "@oceanfresh/supabase",
  "version": "0.0.0",
  "private": true,
  "description": "Supabase client configuration for OceanFresh",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@oceanfresh/shared": "workspace:*",
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "vitest": "^2.0.3"
  }
}
